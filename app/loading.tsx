// This file enables Next.js streaming — the root layout (with splash screen)
// is sent to the browser immediately while page data loads server-side.
export default function Loading() {
  return null;
}
